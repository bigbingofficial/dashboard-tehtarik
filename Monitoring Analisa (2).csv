<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard CSV</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid black;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <h2>Data dari CSV</h2>
    <table id="csvTable"></table>

    <script>
        async function loadCSV() {
            const response = await fetch('data.csv'); // Ganti dengan nama file CSV yang kamu upload
            const data = await response.text();
            const rows = data.split("\n").map(row => row.split(","));
            
            let table = "<tr>";
            rows[0].forEach(header => {
                table += `<th>${header}</th>`;
            });
            table += "</tr>";

            for (let i = 1; i < rows.length; i++) {
                table += "<tr>";
                rows[i].forEach(cell => {
                    table += `<td>${cell}</td>`;
                });
                table += "</tr>";
            }

            document.getElementById("csvTable").innerHTML = table;
        }

        loadCSV();
    </script>
</body>
</html>
